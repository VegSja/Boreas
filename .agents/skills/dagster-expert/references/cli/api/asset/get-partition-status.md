---
title: dg api asset get-partition-status
triggers:
  - "partition materialization status or stats for an asset"
---

```bash
dg api asset get-partition-status <ASSET_KEY>
```

Returns partition materialization statistics (materialized, failed, missing counts) for a partitioned asset.
