---
title: dg api deployment settings-set
triggers:
  - "setting deployment settings in Dagster Plus"
---

Set deployment settings from a YAML file.

```bash
dg api deployment settings-set <FILE>
```

- `<FILE>` — path to a YAML file defining the desired deployment settings
