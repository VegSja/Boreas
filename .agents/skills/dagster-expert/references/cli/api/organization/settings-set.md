---
title: dg api organization settings-set
triggers:
  - "setting organization settings in Dagster Plus"
---

Set organization settings from a YAML file.

```bash
dg api organization settings-set <FILE>
```

- `<FILE>` — path to a YAML file defining the desired organization settings
