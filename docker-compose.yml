services:
  streamlit:
    build:
      context: .
      dockerfile: streamlit_app/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ./boreas.duckdb:/app/boreas.duckdb:ro
    restart: unless-stopped
